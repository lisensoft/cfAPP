// import store from '../store'
/**
 * 判断是否有权限
 * @param {*} code 权限码
 */
export function isHaveMenu(code) {
  return true
  // if (store.getters.userMenu.length > 0) {
  //   if (store.getters.userMenu.filter((item) => { return Number.parseInt(item.code) === code }).length > 0) {
  //     return true
  //   } else {
  //     return false
  //   }
  // }
  // return false
}
